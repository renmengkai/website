---
import { getDesign } from '../lib/design';
import VimaHome from '../themes/vima/pages/home.astro';
import GlassHome from '../themes/glass/pages/home.astro';

const design = getDesign(Astro.cookies);
---

{design === 'glass' ? <GlassHome /> : <VimaHome />}
