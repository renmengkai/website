---
import { getDesign } from '../lib/design';
import VimaNotFound from '../themes/vima/pages/not-found.astro';
import GlassNotFound from '../themes/glass/pages/not-found.astro';

const design = getDesign(Astro.cookies);
---

{design === 'glass' ? <GlassNotFound /> : <VimaNotFound />}
