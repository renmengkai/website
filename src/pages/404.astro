---
import Layout from '../layouts/Layout.astro';
---

<Layout title="页面未找到 - RMK" description="抱歉，您访问的页面不存在">
  <div class="error-page relative min-h-[80vh] flex items-center justify-center overflow-hidden pt-40">
    <!-- 背景装饰 -->
    <div class="absolute inset-0 -z-10">
      <!-- 渐变背景 -->
      <div class="absolute inset-0 bg-gradient-to-b from-primary-50/50 via-transparent to-transparent dark:from-primary-950/20"></div>
      <!-- 装饰圆形 -->
      <div class="floating-circle absolute top-20 left-[10%] w-72 h-72 rounded-full bg-gradient-to-br from-primary-200/30 to-primary-300/20 dark:from-primary-800/10 dark:to-primary-700/5 blur-3xl"></div>
      <div class="floating-circle-reverse absolute bottom-20 right-[10%] w-96 h-96 rounded-full bg-gradient-to-tl from-blue-200/30 to-cyan-300/20 dark:from-blue-800/10 dark:to-cyan-700/5 blur-3xl"></div>
      <!-- 网格背景 -->
      <div class="absolute inset-0 bg-[linear-gradient(rgba(16,185,129,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(16,185,129,0.03)_1px,transparent_1px)] bg-[size:60px_60px] [mask-image:radial-gradient(ellipse_50%_50%_at_50%_50%,black,transparent)]"></div>
    </div>

    <div class="container-custom relative">
      <div class="max-w-3xl mx-auto text-center">
        <!-- 404 主视觉 -->
        <div class="error-visual mb-10">
          <div class="relative inline-block">
            <!-- 外圈装饰 -->
            <div class="absolute inset-0 -m-4 rounded-full border-2 border-dashed border-primary-200/50 dark:border-primary-700/30 animate-spin-slow"></div>
            <div class="absolute inset-0 -m-8 rounded-full border border-primary-100/30 dark:border-primary-800/20"></div>
            
            <!-- 主圆形背景 -->
            <div class="relative w-44 h-44 rounded-full bg-gradient-to-br from-primary-100 via-primary-50 to-white dark:from-primary-900/40 dark:via-primary-900/20 dark:to-dark-300/50 shadow-2xl shadow-primary-500/10 dark:shadow-primary-500/5 flex items-center justify-center">
              <!-- 内圈光效 -->
              <div class="absolute inset-2 rounded-full bg-gradient-to-br from-white/80 to-transparent dark:from-white/5"></div>
              
              <!-- 404 数字 -->
              <span class="relative text-7xl font-black tracking-tight">
                <span class="bg-gradient-to-br from-primary-500 via-primary-600 to-primary-700 bg-clip-text text-transparent drop-shadow-sm">4</span>
                <span class="bg-gradient-to-br from-primary-400 via-primary-500 to-primary-600 bg-clip-text text-transparent">0</span>
                <span class="bg-gradient-to-br from-primary-500 via-primary-600 to-primary-700 bg-clip-text text-transparent drop-shadow-sm">4</span>
              </span>
            </div>
            
            <!-- 装饰小元素 -->
            <div class="absolute -top-2 -right-2 w-6 h-6 rounded-full bg-gradient-to-br from-yellow-300 to-orange-400 shadow-lg shadow-orange-300/50 animate-bounce-slow"></div>
            <div class="absolute -bottom-1 -left-3 w-4 h-4 rounded-full bg-gradient-to-br from-blue-300 to-cyan-400 shadow-lg shadow-cyan-300/50 animate-bounce-slow animation-delay-500"></div>
          </div>
        </div>
        
        <!-- 标题区域 -->
        <div class="mb-8">
          <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-4 tracking-tight">
            哎呀，页面走丢了
          </h1>
          <p class="text-lg lg:text-xl text-gray-500 dark:text-gray-400 max-w-lg mx-auto leading-relaxed">
            您访问的页面可能已被移动、删除，或者从未存在过
          </p>
        </div>
        
        <!-- 操作按钮 -->
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12">
          <a 
            href="/"
            class="group inline-flex items-center gap-2.5 px-7 py-3.5 bg-gradient-to-r from-primary-500 to-primary-600 hover:from-primary-600 hover:to-primary-700 text-white font-semibold rounded-2xl transition-all duration-300 shadow-xl shadow-primary-500/25 hover:shadow-2xl hover:shadow-primary-500/30 hover:-translate-y-0.5"
          >
            <svg class="w-5 h-5 transition-transform group-hover:-translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            返回首页
          </a>
          
          <a 
            href="/blog"
            class="group inline-flex items-center gap-2.5 px-7 py-3.5 bg-white/80 dark:bg-dark-100/80 backdrop-blur-sm border border-gray-200 dark:border-dark-200 hover:border-primary-300 dark:hover:border-primary-700 text-gray-700 dark:text-gray-300 font-semibold rounded-2xl transition-all duration-300 hover:shadow-lg hover:-translate-y-0.5"
          >
            <svg class="w-5 h-5 transition-transform group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" />
            </svg>
            浏览日志
          </a>
        </div>
        
        <!-- 快捷链接 -->
        <div class="inline-flex items-center gap-6 px-6 py-4 rounded-2xl bg-gray-50/80 dark:bg-dark-200/50 backdrop-blur-sm border border-gray-100 dark:border-dark-200">
          <span class="text-sm text-gray-500 dark:text-gray-500">或前往</span>
          <div class="flex items-center gap-4">
            <a href="/projects" class="text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-primary-500 dark:hover:text-primary-400 transition-colors">
              作品集
            </a>
            <span class="w-1 h-1 rounded-full bg-gray-300 dark:bg-gray-600"></span>
            <a href="/about" class="text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-primary-500 dark:hover:text-primary-400 transition-colors">
              关于我
            </a>
          </div>
        </div>
        
        <!-- 底部提示 -->
        <div class="mt-12">
          <p class="text-sm text-gray-400 dark:text-gray-500">
            遇到问题？
            <a href="mailto:contact@mengkai.ren" class="text-primary-500 hover:text-primary-600 font-medium hover:underline underline-offset-2 transition-colors">
              联系我
            </a>
          </p>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* 主视觉浮动动画 */
  .error-visual {
    animation: float 4s ease-in-out infinite;
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-12px);
    }
  }
  
  /* 慢速旋转 */
  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }
  
  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }
  
  /* 弹跳动画 */
  @keyframes bounce-slow {
    0%, 100% {
      transform: translateY(0) scale(1);
    }
    50% {
      transform: translateY(-8px) scale(1.1);
    }
  }
  
  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }
  
  .animation-delay-500 {
    animation-delay: 0.5s;
  }
  
  /* 背景装饰动画 */
  .floating-circle {
    animation: float-bg 8s ease-in-out infinite;
  }
  
  .floating-circle-reverse {
    animation: float-bg-reverse 10s ease-in-out infinite;
  }
  
  @keyframes float-bg {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(20px, -20px) scale(1.05);
    }
  }
  
  @keyframes float-bg-reverse {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(-20px, 20px) scale(1.05);
    }
  }
</style>
