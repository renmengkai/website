---
import { getDesign } from '../lib/design';
import VimaContact from '../themes/vima/pages/contact.astro';
import GlassContact from '../themes/glass/pages/contact.astro';

const design = getDesign(Astro.cookies);
---

{design === 'glass' ? <GlassContact /> : <VimaContact />}
